name: build-sha3-openlane-sky130
on:
  push:
    branches:
      - main
    paths:
      - 'sha3/**'
jobs:
  build-openlane:
    runs-on: ubuntu-latest
    env:
      OPENLANE_ROOT: not_really_used
    steps:
      - name: Checkout examples
        uses: actions/checkout@v2
        with:
          path: openlane-examples
      - name: Checkout pdk
        uses: actions/checkout@v2
        with:
          repository: olofk/pdklite
          path: pdklite
      - run: echo "PDK_ROOT=$GITHUB_WORKSPACE/pdklite" >> $GITHUB_ENV
      - run: echo "EDALIZE_LAUNCHER=${GITHUB_WORKSPACE}/openlane-examples/openlane_runner.py" >> $GITHUB_ENV
      - run: pip3 install --user -e "git+https://github.com/asicbitch/edalize.git@openlane#egg=edalize"
      - run: pip3 install fusesoc
      - run: docker pull efabless/openlane:v0.12
      - run: fusesoc library add sha3 $GITHUB_WORKSPACE/openlane-examples/sha3
      - run: fusesoc run --target=sky130 sha3
      